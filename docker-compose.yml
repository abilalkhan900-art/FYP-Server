version: "3.9"

services:
  client:
    build: .
    container_name: client_container
    volumes:
      - clientvol:/clientdata
    environment:
      - SERVER_IP=SERVER_PUBLIC_IP   # Replace with actual server public IP
      - SERVER_PORT=5000

volumes:
  clientvol:
